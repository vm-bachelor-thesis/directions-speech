import { StyleSheet } from 'react-native';

export const templates = StyleSheet.create({
  messageView: {
    marginLeft: 14,
    marginRight: 14,
    marginBottom: 14,
    borderRadius: 5,
    padding: 14,
  },
  messageTextListening: {
    color: '#aaa',
  },
});
